{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "nixpacks.toml",
    "buildCommand": "bash -c 'composer install --no-interaction --prefer-dist --optimize-autoloader && cd ../frontend && npm install && npm run build'"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "bash -c 'php artisan migrate --force && bash post-deploy.sh && vendor/bin/heroku-php-apache2 public/'"
  }
}